# VMIcons/

Fallback icons for operating systems that don't ship their own `splash.png`
inside their `VMImages/<slug>/` directory.

Name files as `<slug>.{webp,svg,png,jpg,jpeg}` where `<slug>` matches the
directory name under `VMImages/` (e.g. `ubuntu-24.04.webp`).
